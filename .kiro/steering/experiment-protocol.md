---
inclusion: fileMatch
fileMatchPattern: "scripts/run_*"
---

# Experiment Protocol — Parametric Runs & Result Logging

## Seed Management

- **Default seeds**: 42, 43, 44 (thesis uses 3 seeds for statistical significance)
- **Seed 43 is optimal for N=10** (10x better MSE than seed 42)
- Seeds control: NumPy RNG, PyTorch RNG, Python `random` module
- Always set all three: `np.random.seed(s)`, `torch.manual_seed(s)`, `random.seed(s)`
- Report seed in all result JSONs and binnacle entries

## Result Logging Structure

All parametric runs produce:
```
scripts/notebook_results/parametric_run_<timestamp>_<hash>.json
```

Required fields in result JSON:
- `timestamp`, `run_id` (8-char hex)
- `config` — full configuration dict (N, J, p, hidden, epochs, lr, patience, seed, h_test, etc.)
- `phases` — dict with `phase1`, `phase2`, `phase3`, `phase4` sub-dicts
- `success` — boolean (did the run complete without error?)
- `elapsed_s` — total wall-clock time

## Binnacle Protocol

When results are thesis-relevant:
1. Run with `--binnacle --label "description"` flag
2. Auto-appends to `documentation/binnacles/binnacle-N6.md` or `binnacle-N10.md`
3. Include: config table, per-seed results, aggregated mean±std, observations
4. Compare against previous run (auto-generated by `run_notebooks.py`)

## Parametric Run Conventions

### Configuration Naming
- `baseline_N6` — standard N=6 config (h=64, 6000ep, seed=42)
- `baseline_N10` — standard N=10 config (h=128, 6000ep, patience=500, seed=43)
- `dense_grid_N6` — 40-point h-grid (for gradient analysis only)
- `ladder_N6` — non-uniform J ladder topology
- `edge_features_N6` — NNConv with edge features enabled

### Validation Checklist (before committing results)
1. ΔE/gap < 5% at h_test? (primary criterion)
2. Correct phase label? (paramagnetic for h > 1)
3. MSE reasonable? (< 0.01 for N=6, < 0.05 for N=10)
4. No NaN/Inf in outputs?
5. Runtime within expected bounds? (N=6: ~50s, N=10: ~2-6min)

## Script-Specific Notes

### `run_v61_parametric.py`
- Accepts `--configs` flag for selecting which configurations to run
- Produces per-config JSON + optional binnacle entry
- Supports `--n-seeds 3` for multi-seed runs
- Exit codes: 0=all pass, 1=some fail, 2=validation fail

### `run_thesis_results.py`
- Consolidates 15 definitive runs into Tables 4.2 (N=6) and 4.3 (N=10)
- Computes mean±std across seeds for each h_test
- Produces `thesis_results_<timestamp>_<hash>.json`
- Takes ~9 minutes (3 seeds × 5 configs)

### `run_notebooks.py`
- Executes actual Jupyter notebooks with `nbconvert`
- Auto-extracts metrics from cell outputs via regex
- Supports `--timeout` (default 600s), `--keep-last N`, `--dry-run`
- Produces executed notebooks + JSON summaries + optional binnacle

## When to Re-Run

| Change | Re-run |
|--------|--------|
| Modified `vqe_optimizer.py` | `make smoke-test` + `run_v61_parametric.py --configs baseline_N6` |
| Modified `mpnn_predictor.py` | `make smoke-test` + full parametric (all configs) |
| Modified `hardware_deployer_v61.py` | `scripts/smoke_test_v61.py` |
| Modified `analysis_utils.py` | `scripts/smoke_test_v61.py` (includes gradient analysis) |
| New thesis results needed | `scripts/run_thesis_results.py` (~9min) |
